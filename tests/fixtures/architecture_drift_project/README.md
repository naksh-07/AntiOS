# Architecture Drift Project Fixture

Test fixture representing a project whose dependencies and test commands mutated post-installation (e.g., vitest added).
Used to test drift detection, adaptation proposals, and configuration resynchronization.
