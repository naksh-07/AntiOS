{
  "name": "ts-web-client",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
