# Existing Custom Agents Project Fixture

Test fixture with pre-existing user-authored skill and custom agent definition.
Validates that AntiOS compilation and adaptation never silently overwrites or destroys user-authored agent surfaces.
