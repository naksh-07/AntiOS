[project]
name = "math-engine"
version = "0.1.0"
dependencies = ["fastapi>=0.100.0"]

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]

[tool.ruff]
line-length = 88

[tool.mypy]
strict = true
