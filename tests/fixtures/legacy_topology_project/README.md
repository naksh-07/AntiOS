# Legacy Topology Project Fixture

Test fixture demonstrating an archaic non-standard project structure managed via a Makefile and shell scripts without standard package manifests.
Tests fallback boundary compilation, Unknown classification, and manifestless fingerprinting.
