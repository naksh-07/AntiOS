# Stale Intelligence Project Fixture

Test fixture representing a pre-compiled AntiOS instance where recorded metadata points to a deleted component and has an outdated manifest fingerprint.
Validates Phase 60 drift detection, stale path detection, and automatic re-synchronization triggers.
